# Issue 阶段更新（评论模板）

时间（含时区）：

Issue / 当前有效交接标识与评论：

status:

实际当前执行者（未启动写 none）：

分支 / base SHA / head SHA / PR：

本轮已做与证据：

尚未做 / 阻碍：

next_actor:

next_action:

user_action_required:

当前写入是否已停止：

注意：没有 PID、thread、RPC、run 状态要求；这里只是任务事实摘要，不会自动派工。
