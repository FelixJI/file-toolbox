# v4.0 GLM：阻碍与止损

停止继续无方向试错，保留现存代码和测试现场。写 BLOCKED 结果，必须有：指定 Issue/handoff_id、当前分支/SHA 和 dirty 归属、阻碍类别、最小复现、真实报错/命令/退出码、已尝试方案及同根因失败轮数、已排除路线、仍未完成 AC、需要 Codex 判断/处理的具体问题。

权限/网络/额度/环境不可用与产品决策分开；未知明确未知，不擅自更换工具、账号、供应商、付费方式或关闭保护。不要把“应该没问题”写成成功。

写回 Issue；不能写就输出待发布摘要。结尾主动给真实短句：

`按 AI Flow v4.0 处理 #<实际编号> / <实际交接标识> 的 GLM 阻碍，读取 <实际阻碍评论位置>、实际报错与现存改动，由 Codex 调查根因并决定直接解决或重新划定 GLM 施工范围；需要再次转 GLM 时给我一句话，不自动调用、不重复无效尝试。`

之后结束本轮，不调用 Codex，不自主领取其他任务。
